import { Link, useParams } from 'react-router-dom'
import { Card } from '../components/ui/Card'
import { useAppState } from '../lib/AppState'
import { fullName } from '../lib/format'
export function ClientDetailPage(){const {id=''}=useParams();const s=useAppState();const c=s.clients.find(x=>x.id===id);if(!c)return <Card className="p-6">Client not found.</Card>;const animals=s.patients.filter(p=>p.clientId===c.id);return <div className="space-y-5"><div><h2 className="text-2xl font-bold">{fullName(c)}</h2><p className="text-sm text-slate-500">Client record</p></div><Card className="grid gap-4 p-5 md:grid-cols-2"><div><p className="text-xs uppercase text-slate-400">Address</p><p className="mt-1">{c.address}, {c.postcode}</p></div><div><p className="text-xs uppercase text-slate-400">Contact</p><p className="mt-1">{c.phone}<br/>{c.email}</p></div></Card><Card><div className="border-b p-5"><h3 className="font-bold">Linked animals</h3></div><div className="divide-y">{animals.map(p=><Link to={`/patients/${p.id}`} key={p.id} className="block p-5 hover:bg-slate-50"><p className="font-bold">{p.name}</p><p className="text-sm text-slate-500">{p.breed} · {p.species}</p></Link>)}</div></Card></div>}
