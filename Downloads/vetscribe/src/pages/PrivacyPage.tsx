import { Card } from '../components/ui/Card'
export function PrivacyPage(){const rows=[['Client personal data','Stored separately from animal clinical data where practical and accessed only for practice workflows.'],['Access controls','Tenant isolation and role-based access are enforced in the supplied Supabase RLS migration.'],['Audit history','Clinical record creation, update, approval, export and access events can be recorded in the audit log.'],['Data residency','Configure and verify your Supabase/Vercel deployment region before making residency commitments.'],['Retention','Demo default is 5 years; production policy must be chosen by the practice and legal advisers.']];return <div className="space-y-5"><div><h2 className="text-2xl font-bold">Privacy & data handling</h2><p className="text-sm text-slate-500">Implementation placeholders — not certification or legal advice.</p></div><Card className="divide-y">{rows.map(([a,b])=><div key={a} className="p-5"><h3 className="font-bold">{a}</h3><p className="mt-1 text-sm leading-6 text-slate-600">{b}</p></div>)}</Card><div className="rounded-xl border border-slate-200 bg-white p-4 text-sm text-slate-600">VetScribe does not claim GDPR certification, RCVS approval, VMD approval, or any security certification that has not been independently implemented and verified.</div></div>}
