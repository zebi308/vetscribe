import { Link } from 'react-router-dom'
import { Building2, FileText, History, LockKeyhole, Users } from 'lucide-react'
import { Card } from '../components/ui/Card'
export function SettingsPage(){const items=[['Practice','Practice details, branding and retention','/settings/practice',Building2],['Staff','Roles, status and access','/settings/staff',Users],['Templates','Clinical note and owner-summary templates','/settings/templates',FileText],['Privacy','Data handling, access controls and retention','/settings/privacy',LockKeyhole],['Audit log','Clinical record and practice activity','/settings/audit',History]] as const;return <div className="space-y-5"><div><h2 className="text-2xl font-bold">Settings</h2><p className="text-sm text-slate-500">Configure the practice without making unsupported compliance claims.</p></div><div className="grid gap-4 md:grid-cols-2">{items.map(([title,desc,to,Icon])=><Link to={to} key={to}><Card className="h-full p-5 transition hover:border-brand-300"><Icon className="text-brand-700"/><h3 className="mt-4 font-bold">{title}</h3><p className="mt-1 text-sm text-slate-500">{desc}</p></Card></Link>)}</div></div>}
