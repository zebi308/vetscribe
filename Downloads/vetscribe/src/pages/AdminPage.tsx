import { Card } from '../components/ui/Card'
import { useAppState } from '../lib/AppState'
export function AdminPage(){const s=useAppState();return <div className="space-y-5"><div><h2 className="text-2xl font-bold">Super admin</h2><p className="text-sm text-slate-500">Platform-level usage only. Avoid exposing individual clinical data unnecessarily.</p></div><div className="grid gap-4 md:grid-cols-4">{[['Practices',1],['Active Users',s.profiles.filter(x=>x.isActive).length],['Consultations This Month',s.consultations.length],['AI Notes Generated',s.consultations.filter(x=>x.clinicalNote).length]].map(([a,b])=><Card className="p-5" key={a}><p className="text-sm text-slate-500">{a}</p><p className="mt-2 text-3xl font-bold">{b}</p></Card>)}</div><Card className="p-5"><p className="font-bold">Oakwood Veterinary Practice</p><p className="mt-1 text-sm text-slate-500">Manchester · {s.profiles.length} users · {s.consultations.length} consultations · Active</p></Card></div>}
